# Control Adapters

Adapters that execute validated actions against an environment live here.

Initial target:

- BlueStacks through ADB touch commands

The core action schema should remain independent from ADB syntax.

